function clearText() {
  console.log("in clearText function");
  // if (field.defaultValue == field.value) {
  //   field.value = '';
  // } else if (field.value == '') {
  //   field.value = field.defaultValue;
  // }
  // return field;
}

export {clearText};
